public String aOrAn(String word) {
    //code here
    List<String> vowels = new List<String>{'a', 'e', 'i', 'o', 'u'};

    if(String.isBlank(word)){return '';}
    if(vowels.contains(word.subString(0,1).toLowerCase())){
        return 'an '+word;
    }
    return 'a '+word;
}